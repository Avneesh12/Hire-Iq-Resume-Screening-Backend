# Resume-Screening
Developed an intelligent resume screening system using FastAPI and machine learning models (TF-IDF + BiLSTM) to automate candidate evaluation. The system parses resumes (PDF/DOCX), extracts features, and predicts candidate relevance using an ensemble approach, improving screening efficiency and accuracy.
